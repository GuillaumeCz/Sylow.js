/* eslint-env node, mocha */

import assert from 'assert';

// import Sylow from './lib/sylow';

describe('Sylow', () => {
  describe('...Connection', () => {
    it('...should return a instance of class Sylow', () => {
      assert.equal('Hello', 'Hello');
    });
  });

  describe('TestMe', () => {
    it('...should go ok', () => {
      assert.equal(1, 2 - 1);
    });
  });
});
